<h1>Editing Home</h1>

<%= form_for(@home) do |f| %>
  <% if @home.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(home.errors.count, "error") %> prohibited this home from being saved:</h2>

      <ul>
      <% home.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    先生: 確認コード<br />
    <%= f.password_field :home_item_request_teacher %>
  </div>


  <div class="field">
    助手: 確認コード<br />
    <%= f.password_field :home_item_request_assistant %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<%= link_to 'Show', @home %> |
<%= link_to 'Back', homes_path %>
