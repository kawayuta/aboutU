<p id="notice"><%= notice %></p>
<% if user_signed_in? %>
                <strong><%= link_to current_user.nickname, user_path(current_user) %></strong>
                <%= link_to 'プロフィール変更', edit_user_registration_path %>
                <%= link_to '情報登録', items_path %>
                <%= link_to 'ログアウト', destroy_user_session_path, method: :delete %>
<% else %>
                <%= link_to 'サインアップ', new_user_registration_path %>
                <%= link_to 'ログイン', new_user_session_path %>
<% end %>
  <br />
  <br />
<% if user_signed_in? %>
  <% @following_user.each do |x| %>
  <br />
   <% if About.find_by(["my_id = ? and user_id = ? and about_id = ?",current_user.id,x.id, 1]) && About.find_by(["my_id = ? and user_id = ? and about_id = ?",x.id,current_user.id, 1])%>
    💕
     <% elsif About.find_by(["my_id = ? and user_id = ? and about_id = ?",current_user.id,x.id, 1]) && About.find_by(["my_id = ? and user_id = ? and about_id = ?",x.id,current_user.id, 2])%>
    🌟
     <% elsif About.find_by(["my_id = ? and user_id = ? and about_id = ?",current_user.id,x.id, 1]) && About.find_by(["my_id = ? and user_id = ? and about_id = ?",x.id,current_user.id, 3])%>

    <% elsif About.find_by(["my_id = ? and user_id = ? and about_id = ?",current_user.id,x.id, 1]) && About.find_by(["my_id = ? and user_id = ? and about_id = ?",x.id,current_user.id, 3])%>

    <% else %>

 <% end %>
  <%= x.nickname %>

          <% @user_id = params[:id] %>
          <% @my_id = current_user.id%>

          <% if About.find_by(["my_id = ? and user_id = ? and about_id = ?", @my_id, x.id, 1])%>
          好きな人
          <% end %>

          <% if About.find_by(["my_id = ? and user_id = ? and about_id = ?", @my_id, x.id, 2])%>
          親友
          <% end %>

          <% if About.find_by(["my_id = ? and user_id = ? and about_id = ?", @my_id, x.id, 3])%>
          友達
          <% end %>

          <% if About.find_by(["my_id = ? and user_id = ? and about_id = ?", @my_id, x.id, 4])%>
          知り合い
          <% end %>

    <% end %>

    <% end %>